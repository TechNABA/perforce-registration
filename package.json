{
  "name": "perforce-registration",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for the NABA Perforce onboarding form",
  "scripts": {
    "test": "node test/worker.test.mjs",
    "deploy": "wrangler deploy"
  }
}
